require('./overrides.sass')

const preset = {
  theme: {
    themes: {
      light: {
        primary: '#FFDE03',
        secondary: '#0336FF',
        accent: '#FF0266',
      },
    },
  },
}

module.exports = { preset }
